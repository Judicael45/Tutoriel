{% extends 'base.html.twig' %}

{% block title %}Entity{% endblock %}

{% block body %}
    <h1>Entity</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Return</th>
                <td>{{ entity.return }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_entity_index') }}">back to list</a>

    <a href="{{ path('app_entity_edit', {'id': entity.id}) }}">edit</a>

    {{ include('entity/_delete_form.html.twig') }}
{% endblock %}
